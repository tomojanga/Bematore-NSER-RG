-r base.txt

# Production Server
gunicorn==23.0.0
gevent==23.9.1

# Database Support - Both PostgreSQL and MySQL
# PostgreSQL (recommended for VPS/cloud deployments)
psycopg2-binary==2.9.10

# MySQL (for cPanel deployment)
mysqlclient==2.2.1
# Alternative: PyMySQL==1.1.1 (if mysqlclient fails to install)

# Production Monitoring
newrelic==9.4.0
datadog==0.48.1

# Security
django-defender==0.9.7
django-ratelimit==4.1.0
django-csp==3.7

# Performance
django-cachalot==2.6.1

